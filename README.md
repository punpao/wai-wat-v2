# กาลวัฒน์ (Kala-Wat) — AR Wisdom Trail

ต้นแบบ (prototype) สำหรับ **Moral Hackathon 2026** — แอปเดินสำรวจสถานที่จริงแบบ Pokémon GO
แต่สิ่งที่ตามหาคือ **ผู้สูงอายุและเรื่องเล่าประจำย่าน** ไม่ใช่มอนสเตอร์

เมื่อผู้สำรวจเดินไปถึงจุดตรวจ (checkpoint) จะเปิดกล้องสแกนหา “ผู้สูงอายุ” ที่ปรากฏขึ้นในโหมด AR
แล้วเล่าความรู้ที่ผูกกับสถานที่นั้นให้ฟัง ส่วนผู้สูงอายุที่เล่าเรื่องจะได้ **แต้มภูมิปัญญา**
ซึ่งนำไปแลกบริการสวัสดิการในชีวิตจริงได้ เช่น พาไปโรงพยาบาล หรือตัดผมถึงบ้าน

> นี่คือ **ต้นแบบสำหรับสาธิต** ไม่ใช่ระบบใช้งานจริง — ไม่มี backend ไม่มีฐานข้อมูล ไม่มีระบบล็อกอิน
> ข้อมูลทั้งหมดเป็น mock และเก็บไว้ใน `localStorage` ของเบราว์เซอร์เครื่องนั้น

---

## วิธีรัน

```bash
npm install
npm run dev
```

เปิด `http://localhost:5173` — **ไม่ต้องใช้ API key และไม่ต้องตั้งค่า environment variable ใด ๆ**
ใช้ได้ทั้งบนเบราว์เซอร์เดสก์ท็อปและมือถือ

```bash
npm run build && npm run preview   # ทดสอบเวอร์ชัน production
```

### Deploy บน Vercel

โปรเจกต์นี้พร้อม deploy ตามค่าเริ่มต้น — มี `vercel.json` กำหนดไว้ให้แล้ว

1. push โค้ดขึ้น GitHub
2. ที่ Vercel เลือก **Add New → Project** แล้วเลือก repo นี้
3. ค่าที่ Vercel จะตรวจจับได้เองและถูกต้องอยู่แล้ว: Framework `Vite` ·
   Build `npm run build` · Output `dist`
4. กด **Deploy** — **ไม่ต้องตั้ง environment variable ใด ๆ**

หรือใช้ CLI:

```bash
npx vercel        # preview
npx vercel --prod # production
```

`vercel.json` ทำสองอย่าง: rewrite ทุกเส้นทางไปที่ `index.html` (กัน 404 เวลาเปิดลิงก์ตรง)
และส่ง header `Permissions-Policy` ที่อนุญาต `camera`, `geolocation`, `gyroscope`,
`accelerometer`, `magnetometer` ให้กับโดเมนตัวเอง ซึ่งจำเป็นสำหรับหน้าสแกน AR

---

## การใช้งานบนมือถือ (สำคัญก่อนสาธิตจริง)

หน้าสแกน AR ใช้ฮาร์ดแวร์ของเครื่องจริง 3 อย่าง และทุกอย่างต้องเปิดผ่าน **https**
(URL ที่ Vercel ให้มาเป็น https อยู่แล้ว — แต่ถ้าเปิดจาก `http://` หรือ IP ในวง LAN
เบราว์เซอร์จะบล็อกกล้องและเข็มทิศทั้งหมด)

| ฮาร์ดแวร์ | ใช้ทำอะไร | ถ้าใช้ไม่ได้จะเกิดอะไร |
|---|---|---|
| **กล้องหลัง** | ภาพพื้นหลังของหน้าสแกน | ขึ้นข้อความบอกสาเหตุ + ปุ่ม “ลองเปิดกล้องอีกครั้ง” และใช้พื้นหลังจำลองแทน ขั้นตอนอื่นทำงานปกติ |
| **GPS** | ระยะห่างจากจุดตรวจ | กลับไปใช้ทิศทางจำลอง |
| **เข็มทิศ** | ทิศ “ซ้าย / ขวา / ข้างหลัง” ของจริง | กลับไปใช้ทิศทางจำลอง |

**ซ้าย / ขวา / ข้างหลัง ติดตามทิศจริงอย่างไร**
เมื่อมีทั้ง GPS จริงและเข็มทิศ แอปจะคำนวณ bearing จากตำแหน่งผู้ใช้ไปยังพิกัดจุดตรวจ
แล้วลบด้วยทิศที่เครื่องหันอยู่จริง ได้เป็นมุมที่ต้องหมุนตัว → แปลงเป็น
**ตรงไป / ซ้าย / ขวา / ข้างหลัง** ลูกศรจะหมุนตามเครื่องแบบเรียลไทม์
เมื่อเดินเข้าใกล้เหลือไม่เกิน **25 เมตร** และหันหน้าไปทางจุดตรวจ ผู้สูงอายุจะปรากฏขึ้นเอง
มุมขวาบนจะบอกเสมอว่าตอนนี้ใช้ **“เข็มทิศจริง”** หรือ **“ทิศทางจำลอง”** อยู่

**บน iPhone** ต้องกดปุ่ม **“เปิดเข็มทิศ เพื่อชี้ทิศทางจริง”** บนหน้าสแกนก่อน
เพราะ iOS บังคับให้ขออนุญาตเซ็นเซอร์จากการกดปุ่มเท่านั้น (Android เปิดให้อัตโนมัติ)

**อย่าเปิดในเบราว์เซอร์ในแอป** เช่นเบราว์เซอร์ที่อยู่ใน LINE, Facebook หรือ Instagram
เพราะหลายตัวไม่ยอมให้ใช้กล้อง — ให้กด “เปิดใน Safari” หรือ “เปิดใน Chrome” ก่อน

> **หมายเหตุเรื่องแผนที่:** ชั้น “แผนที่จริง” ดึงภาพแผนที่จาก OpenStreetMap จึงต้องต่ออินเทอร์เน็ต
> ถ้าเน็ตถูกจำกัด แอปจะขึ้นข้อความแจ้งและยังกดจุดตรวจได้ตามปกติ — หรือสลับไปชั้น
> **“นักสำรวจ”** ซึ่งวาดเองทั้งหมดและทำงานได้แม้ออฟไลน์

---

## โหมดสาธิต (Demo Mode) — สิ่งที่ต้องรู้ก่อนนำเสนอ

กรรมการจะไม่เดินไปยังพิกัด GPS จริงระหว่างตัดสิน จึงมี **โหมดสาธิต** ให้จำลองการเข้าใกล้จุดตรวจได้ทันที

**ทางที่เร็วที่สุด: ปุ่มกลม ๆ ตรงกลางแถบเมนูล่าง (“สาธิต AR”)**
กดครั้งเดียวเข้าหน้าสแกน AR ทันที ระบบจะเลือกจุดตรวจที่ยังไม่ค้นพบให้เอง
(ถ้าอยู่หน้างานจริงและอยู่ใกล้จุดตรวจอยู่แล้ว จะเข้าสแกนจุดนั้นด้วย GPS จริงโดยไม่จำลองตำแหน่ง)

**ปุ่ม “เลือกจุด”** มุมขวาล่าง สำหรับเลือกจุดตรวจเองหรือรีเซ็ตข้อมูล กดแล้วจะได้:

| สิ่งที่ทำได้ | ผลลัพธ์ |
|---|---|
| **ฉันอยู่ใกล้ \<ชื่อจุด\>** | ย้ายตำแหน่งจำลองไปข้างจุดนั้น แล้วเปิดหน้าสแกน AR ทันที |
| **ใช้ GPS จริง / ล้างตำแหน่งจำลอง** | สลับระหว่างพิกัดจริงกับพิกัดจำลอง (พิกัดจำลองจะปิดการติดตามเข็มทิศจริงเสมอ) |
| **รีเซ็ตข้อมูลทั้งหมด** | ล้าง `localStorage` เพื่อเริ่มสาธิตรอบใหม่ |

อีกทางหนึ่ง: แตะจุดตรวจบนแผนที่ แล้วกดปุ่ม **“โหมดสาธิต · จำลองว่ายืนอยู่ตรงนี้”** ในการ์ดข้อมูลจุดนั้น

**สลับบทบาท** ผู้สำรวจ ⇄ ผู้สูงอายุ ได้จากปุ่มมุมขวาบนของหัวแอป — เป็นความสะดวกในการสาธิตเท่านั้น ไม่ใช่ระบบยืนยันตัวตน

---

## เส้นทางการสาธิตที่แนะนำ (ประมาณ 3 นาที)

1. **แผนที่** — เลือก “เชียงใหม่ · ย่านเมืองเก่า” จากรายการ แผนที่จะบินไปที่จุดตรวจ
2. สลับปุ่ม **แผนที่จริง ⇄ นักสำรวจ** เพื่อให้เห็นว่าเป็นข้อมูลชุดเดียวกันคนละสไตล์
3. แตะบ้านหลังใดก็ได้ → อ่านคำแนะนำของสถานที่ → กด **“จำลองว่ายืนอยู่ตรงนี้”**
   (หรือกดปุ่มกลม **“สาธิต AR”** ตรงกลางแถบเมนูล่าง เพื่อข้ามไปหน้าสแกนทันที)
4. **หน้าสแกน AR** — อนุญาตให้ใช้กล้อง แล้วกดปุ่มตามทิศที่ขึ้น (“ซ้าย!”, “ขวา!”, “ข้างหลัง!”)
   สัญญาณจะร้อนขึ้นเรื่อย ๆ จนพบผู้สูงอายุ *(ถ้าไม่เปิดกล้อง จะใช้พื้นหลังจำลองแทนและขั้นตอนอื่นยังทำงานปกติ)*
   — ถ้าอยู่หน้างานจริงบนมือถือ ให้ข้ามโหมดสาธิตแล้วเดินตามลูกศรได้เลย ดูหัวข้อ “การใช้งานบนมือถือ”
5. ฟังคลิปจนจบ → ระบบบันทึกการค้นพบและให้แต้ม
6. **ส่งกำลังใจทันทีในหน้าเดียวกัน** — กด **“ถูกใจ”** หรือแตะชิป “ขอบคุณที่เล่า” / “อยากฟังอีก”
   ส่งได้ในแตะเดียว และมีช่องพิมพ์เปิดรออยู่แล้วถ้าอยากเขียนเอง
7. **โปรไฟล์** — ดูสถิติ เหรียญตรา และประวัติ (ส่งกำลังใจเพิ่มจากที่นี่ได้อีกทีหลัง)
8. สลับเป็นบทบาท **ผู้สูงอายุ** → เลือกผู้สูงอายุท่านที่เพิ่งฟังในกล่องท้ายหน้าหลัก → เปิด **คลิปของฉัน**
   จะเห็นข้อความที่เพิ่งส่งไปเมื่อครู่ปรากฏอยู่จริง
9. **แลกแต้ม** — แลก “ตัดผมที่บ้าน” แล้วดูหน้ายืนยันและสถานะคำขอ

---

## บทบาทและหน้าจอ

**ผู้สำรวจ (User)**
- **แถบเมนูล่าง** — 4 ช่อง: แผนที่ · เวิร์คช็อป · ปุ่ม “สาธิต AR” ยกนูน · โปรไฟล์
- **เวิร์คช็อป** — คลาสที่ผู้สูงอายุเปิดสอนเอง ยังไม่เลือกบริเวณจะขึ้นคลาสที่กำลังมาแรงข้ามภาค
  เลือกบริเวณแล้วจะเหลือเฉพาะย่านนั้น แต่ละใบเลือกได้ทั้ง “ลงทะเบียน” และ “ดูรายละเอียด”
  หน้ารายละเอียดมี 2 แท็บ คือรายละเอียดคลาส กับข้อมูลผู้สอนทุกคน
  และลิงก์ไป-กลับกับหมุดบนแผนที่ได้สองทาง
- **แผนที่** — เลือกย่านจากรายการ, จุดตรวจเป็นไอคอนบ้าน, แตะเพื่อดูคำแนะนำ, สลับได้ 2 ชั้นแผนที่
- **สแกน AR** — กล้องจริงผ่าน `getUserMedia` + คำใบ้ทิศทางจำลอง + ผู้สูงอายุปรากฏพร้อมคลิปความรู้
- **ส่งกำลังใจ** — อยู่ในเส้นทางเลย ทันทีที่ฟังคลิปจบ ไม่ต้องเดินย้อนไปหน้าประวัติ
  มีปุ่ม “ถูกใจ” ชิปข้อความสำเร็จรูป และช่องพิมพ์ที่เปิดรออยู่แล้ว รวมถึงแชร์ต่อทาง LINE
  ส่งซ้ำจากหน้าประวัติภายหลังได้เช่นกัน (เป็นคอมโพเนนต์เดียวกัน)
- **โปรไฟล์** — จำนวนจุดที่ค้นพบ, สถิติต่อเนื่อง, ระดับ, เหรียญตรา และประวัติที่ฟังซ้ำได้

**ผู้สูงอายุ (Elder)** — หน้าจอชุดนี้ตั้งใจออกแบบให้ **ตัวใหญ่ขึ้น คอนทราสต์สูงขึ้น ปุ่มกดง่ายขึ้น และมีแค่ 3 แท็บ**
- **หน้าหลัก** — แต้มคงเหลือ, สถิติผู้ฟัง, กำลังใจล่าสุด
- **คลิปของฉัน** — คลิปที่เล่าไว้ พร้อมจำนวนคนฟัง ถูกใจ และข้อความจากผู้ฟัง
- **แลกแต้ม** — แลกบริการสวัสดิการ พร้อมหน้ายืนยันและสถานะคำขอ

---

## การลงทะเบียนเวิร์คช็อป

เลือกจ่ายได้สองทาง

| วิธี | เกิดอะไรขึ้นจริงในโค้ด |
|---|---|
| **แต้มภูมิปัญญา** | หักแต้มที่สะสมจากการค้นพบจุดตรวจออกจริง ถ้าแต้มไม่พอปุ่มจะถูกปิดพร้อมบอกว่าขาดอีกเท่าไร ยกเลิกแล้วคืนแต้มให้ |
| **เงินบาท** | **เป็นการจำลองเท่านั้น** — หน้าจอชำระเงินมีคิวอาร์ที่วาดขึ้นเอง ไม่มีการเชื่อมต่อผู้ให้บริการชำระเงินใด ๆ และไม่มีการตัดเงินจริง บันทึกไว้แค่ยอดในเครื่องเพื่อให้ใบจองแสดงผลได้ |

ที่นั่งมีจำนวนจำกัดต่อคลาส เมื่อเต็มปุ่มจะขึ้นว่า “ที่นั่งเต็มแล้ว”
รายการที่จองไว้ทั้งหมดดูได้ในหน้าโปรไฟล์

---

## แก้ไขข้อมูลจำลอง

ข้อมูลทั้งหมดอยู่ในโฟลเดอร์เดียว: `src/data/`

| ไฟล์ | เนื้อหา |
|---|---|
| `locations.js` | ย่าน/เส้นทาง, จุดตรวจ (lat/lng), คำแนะนำ, และคลิปความรู้ของแต่ละจุด |
| `elders.js` | ผู้สูงอายุแต่ละท่าน พร้อมสีของตัวละครที่ใช้วาดภาพประกอบ |
| `rewards.js` | รายการบริการที่แลกได้, เหรียญตรา, และเกณฑ์ระดับ |
| `workshops.js` | เวิร์คช็อปทั้งหมด ผูกกับ `checkpointId` และ `elderIds` (ผู้สอนได้หลายคน) |

เพิ่มจุดตรวจใหม่ = เพิ่ม object ใน `checkpoints` ของย่านนั้น แล้วจุดจะไปโผล่ครบทั้งสองชั้นแผนที่
รายการจุดตรวจ และแผงโหมดสาธิต โดยไม่ต้องแก้ที่อื่น

การอ่าน/เขียนข้อมูลทั้งหมดผ่าน `src/lib/storage.js` ที่เดียว (ห่อ `localStorage` ไว้)
ถ้าจะต่อ backend จริงในอนาคต เปลี่ยนแค่ไฟล์นี้ไฟล์เดียว

---

## เทคโนโลยี

React + Vite + React Router · Tailwind CSS v4 · Leaflet.js + OpenStreetMap ·
`getUserMedia` สำหรับภาพจากกล้อง · `Geolocation.watchPosition` +
`DeviceOrientationEvent` สำหรับทิศทางจริง · `localStorage` สำหรับข้อมูลทั้งหมด

## สิ่งที่ต้นแบบนี้ตั้งใจ "ไม่ทำ"

- **ไม่มี backend / ฐานข้อมูล / ระบบล็อกอิน** — ข้อมูลอยู่ในเครื่องผู้ใช้เท่านั้น
- **ไม่ใช่ AR ด้วย computer vision จริง** — เป็นภาพจากกล้อง + UI ซ้อนทับ + ตัวละคร 2D
  ทิศทางคำนวณจาก GPS และเข็มทิศของเครื่อง ไม่ได้ตรวจจับหรือติดตามวัตถุผ่านเลนส์
  และตัวละครไม่ได้ยึดติดกับพื้นผิวจริง
- **ไม่มีระบบชำระเงินจริง** — ตัวเลือก “จ่ายเป็นเงิน” ของเวิร์คช็อปเป็นหน้าจอจำลองล้วน ๆ
  ติดป้ายกำกับไว้ชัดเจนในแอป ไม่มีการเชื่อมต่อผู้ให้บริการชำระเงินและไม่มีการเก็บข้อมูลบัตร
- **ไม่ใช้ LINE Messaging API** — ปุ่มแชร์ใช้ LINE share URL scheme ซึ่งไม่ต้องมีเซิร์ฟเวอร์หรือ API key
  ถ้าเปิดไม่ได้จะคัดลอกข้อความให้แทนพร้อมแจ้งเตือน
- **ไม่ใช่แผนที่ 3 มิติจริง** — ชั้น “นักสำรวจ” คือการวาดข้อมูลชุดเดิมด้วย SVG ในสไตล์เกม
